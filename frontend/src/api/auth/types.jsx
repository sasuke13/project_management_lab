//login
// Визначення об'єкту для запиту на вхід
const loginRequest = {
    email: 'email',
    password: 'password'
  };
  
  // Визначення об'єкту для відповіді на вхід
  const loginResponse = {
    accessToken: 'access_token'
  };